# 历史故障案例：交换机互联光模块退化

## 故障现象

汇聚交换机与接入交换机之间的物理链路保持 `up`，但业务出现间歇性丢包。两端接口的 CRC 和输入错误计数在同一采样窗口持续增长，接收光功率低于设备告警阈值。

## 根因与处理

只读检查排除了接口管理关闭和配置变更后，维护人员判断光模块或光纤链路发生退化。经变更评审和人工批准，在维护窗口更换异常光模块并清洁跳纤端面。

## 验证标准

更换后持续观察丢包率、CRC 增量、输入错误增量、接收光功率和链路稳定性。只有这些指标均恢复正常，才关闭故障工单；否则回退模块并继续检查跳纤和对端端口。
